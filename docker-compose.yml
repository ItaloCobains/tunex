services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: tunex
      POSTGRES_PASSWORD: tunex
      POSTGRES_DB: tunex
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
